package MethodOverodingu;

public class Main {
    public static void main(String[] args) {
        // declaring a method in sub class which is already present in parent class, its
        // done tocgive child class its own implementation.

        Dog kutta = new Dog();
        kutta.speak();
    }
}
